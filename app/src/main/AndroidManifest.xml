<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.tabletopics">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:appComponentFactory="androidx"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar"
        tools:replace="android:appComponentFactory">
        <activity android:name=".AfterSpeechActivity" />
        <activity android:name=".SpeakingActivity" />
        <activity android:name=".LoadingScreenActivity" />
        <activity android:name=".TopicsListActivity" />
        <activity android:name=".ProfileActivity" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".SpeakNowActivity"
            android:label="Speak Now" />
        <activity
            android:name=".LeaderboardActivity"
            android:label="Leaderboard" />
        <activity
            android:name=".TopicsBankActivity"
            android:label="Topics Bank" />
        <activity
            android:name=".CoachingActivity"
            android:label="Coaching" />
    </application>

</manifest>